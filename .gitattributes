# Normaliza los finales de línea: LF en el repositorio, independientemente del
# sistema en el que se trabaje. Evita que un checkout en Windows haga fallar
# `ruff format --check` en la CI de Linux.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
*.ico binary
